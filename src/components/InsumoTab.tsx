import React, { useState } from 'react';\n\nconst InsumoTab: React.FC = () => {\n    const [products, setProducts] = useState<{ id: number; name: string; unitType: 'KG' | 'PZA'; quantity: number; }[]>([]);\n    const [name, setName] = useState('');\n    const [unitType, setUnitType] = useState<'KG' | 'PZA'>('KG');\n    const [quantity, setQuantity] = useState(0);\n\n    const addProduct = () => {\n        const newProduct = { id: products.length + 1, name, unitType, quantity };\n        setProducts([...products, newProduct]);\n        // Reset form\n        setName('');\n        setUnitType('KG');\n        setQuantity(0);\n    };\n\n    return (\n        <div>\n            <h1>Products Inventory</h1>\n            <input type='text' placeholder='Product Name' value={name} onChange={(e) => setName(e.target.value)} />\n            <select value={unitType} onChange={(e) => setUnitType(e.target.value as 'KG' | 'PZA')}>\n                <option value='KG'>KG</option>\n                <option value='PZA'>PZA</option>\n            </select>\n            <input type='number' placeholder='Quantity' value={quantity} onChange={(e) => setQuantity(Number(e.target.value))} />\n            <button onClick={addProduct}>Add Product</button>\n            <ul>\n                {products.map(product => (\n                    <li key={product.id}>{product.name} - {product.unitType}: {product.quantity}</li>\n                ))}\n            </ul>\n        </div>\n    );\n};\n\nexport default InsumoTab;